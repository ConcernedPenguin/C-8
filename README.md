# C-8
C-8 is a CHIP-8 emulator built entirely in C, using SDL2 as its primary rendering and system interface library. The project emulates the original CHIP-8 virtual machine, including its CPU, memory, instruction set, and display system, allowing classic CHIP-8 programs (ROMs) to run on modern systems.
